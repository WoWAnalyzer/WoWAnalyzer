/**
 * NAME: {
 *   id: number,
 *   name: string,
 *   icon: string,
 *   quality: number,
 * },
 */
import { itemIndexableList } from 'common/ITEMS/Item';

const items = itemIndexableList({});
export default items;
