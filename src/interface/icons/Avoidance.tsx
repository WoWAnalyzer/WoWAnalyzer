import { default as Icon, IconProps } from 'interface/Icon';
import React from 'react';

const icon = (props: IconProps) => <Icon icon="ability_rogue_quickrecovery" {...props} />;
export default icon;
