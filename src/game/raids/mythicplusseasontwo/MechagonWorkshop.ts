import { buildBoss } from '../builders';
import background from './backgrounds/MechagonWorkshop.jpg';

export default buildBoss({
  id: 112098,
  name: 'Operation: Mechagon - Workshop',
  background,
});
