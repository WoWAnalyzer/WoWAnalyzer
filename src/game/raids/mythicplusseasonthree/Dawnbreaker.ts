import { buildBoss } from 'game/raids/builders';
import background from './backgrounds/Dawnbreaker.jpg';

export const Dawnbreaker = buildBoss({
  id: 62662,
  name: 'The Dawnbreaker',
  background,
});
