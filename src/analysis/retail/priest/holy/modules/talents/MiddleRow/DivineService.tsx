import Analyzer, { Options } from 'parser/core/Analyzer';

class DivineService extends Analyzer {
  constructor(options: Options) {
    super(options);
    throw new Error('not Implemented');
  }
}
export default DivineService;
