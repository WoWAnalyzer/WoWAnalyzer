import React from 'react';
import ArmoryIcon from 'interface/icons/Armory';
import { Build } from 'parser/Config';

export default {
  name: 'Standard Build',
  url: 'standard',
  icon: <ArmoryIcon />,
} as Build;
