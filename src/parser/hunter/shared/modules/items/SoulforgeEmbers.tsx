import Analyzer from 'parser/core/Analyzer';

/**
 * Launching a Flare into your Tar Trap causes all enemies inside of the Tar Trap to burn for (900% of Attack power) Fire damage over 12 sec.
 *
 * Example log:
 *
 */
class SoulforgeEmbers extends Analyzer {

}

export default SoulforgeEmbers;
