Use TypeScript + React patterns already present in repo; keep changes minimal and follow real in-repo examples.
Prefer existing analyzer/module patterns from .github/copilot instructions.
Project uses Emotion today in places, but Sass/CSS modules tooling is available.
Validation guidance from repo instructions: use pnpm typecheck / pnpm lint / pnpm build as appropriate; avoid pnpm test unless asked.
